[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    live: POST to the deployed Flask API (opt-in: ACCOUNTING_AGENT_LIVE=1)
    openai: Call OpenAI Responses API (opt-in: ACCOUNTING_AGENT_OPENAI=1)
